language: python
python:
  - "3.7"
  - "3.8"
# command to install dependencies
install:
  - pip install pipenv
  - pipenv install --dev
# command to run tests
script: python -m unittest -vv test_bot

